// Em mlab5/src/assets/js/router.js

const routes = {
    // As rotas serão definidas aqui
};

function router() {
    // A lógica do roteador virá aqui
}

// Adiciona um ouvinte para o evento hashchange
window.addEventListener('hashchange', router);

// Chama o roteador na carga inicial da página
window.addEventListener('load', router);
